
@{
    ViewData["Title"] = "Account";
}

<div class="container">
    <div class="space">
        <div>
            <h4>Account for</h4>
            <p>filip@AppDomain.com</p>
            <a class="btn-theme" asp-action="SignOut" asp-controller="Auth">Sign out</a>
        </div>
    </div>
</div>